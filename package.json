{
  "name": "ssm-fax-web",
  "version": "0.2.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "adm-zip": "^0.5.9",
    "shelljs": "^0.8.4"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "version": "node scripts/change-version.js",
    "stcheck": "stcheck"
  }
}
